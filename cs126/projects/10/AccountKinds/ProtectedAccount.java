// ProtectedAccount.java is another basic class of a package of // bank accounts.// It encorporates a pin protection on the accounts//// Written by: Charles Hoot, for Hands On Java.// Date: July 2000// **************************************************************package AccountKinds;import AccountKinds.AccountSecurityException;abstract class ProtectedAccount extends BasicAccount {		private String myPIN;		// ***************************************************************	// ProtectedAccount constructor	//	// Input: name of account holder, and PIN	// PostCondition: superclass constructor called	//					PIN set	//****************************************************************	public ProtectedAccount ( String name, String pin) {		super(name);		myPIN = pin;	}		// ***************************************************************	// basic method to do a deposit	// input: amount to deposit, PIN	// 	//****************************************************************	public void deposit( double amount, String pin) {		if(!myPIN.equals(pin))			throw new AccountSecurityException("PIN mismatch");		else			deposit(amount);			}		// ***************************************************************	// basic method to do a withdraw	// input: amount to withdraw, PIN	// 	//****************************************************************	public void withdraw( double amount, String pin) {		if(!myPIN.equals(pin))			throw new AccountSecurityException("PIN mismatch");		else			withdraw(amount);			}		}